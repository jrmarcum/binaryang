# TypeScript build output
/dist
/node_modules

# Test coverage
/coverage

# Editor
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Submodule working trees. These are tracked via .gitmodules (gitlinks), so
# git itself ignores this section for staging purposes — listing them here
# just nudges editors and search tools (ripgrep, deno fmt/lint, IDE indexers)
# to skip them when scanning the workspace.
/upstream/
/binaryen-ts/
/wasmtk/

# Private working notes — kept local, not published. CLAUDE.md holds internal
# project context that we don't intend to ship publicly. (The phase/decision
# log formerly at /TASKS.md was moved into cmem/tasks.md — committed, portable
# project memory — on 2026-06-09.)
/CLAUDE.md
